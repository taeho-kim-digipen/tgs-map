# TGS 2026 지도 프로젝트 인계

먼저 `handoff/HANDOFF.md`와 `README.md`를 읽고 기존 구현에서 이어서 작업한다.

- 사용자 언어: 한국어. 짧고 사실 위주로 보고한다.
- 현재 요청은 Codex로 작업을 이관하고 이어서 개발하려는 것이다.
- 런타임 소스는 `dist/` 자체다. 빌드 산출물로 오해해 삭제하거나 새 프로젝트로 덮어쓰지 않는다.
- 지도, 부스 이름/번호/좌표는 공식 PDF에 근거한다. 추측 위치나 생성형 이미지로 대체하지 않는다.
- 메인 전시관 1–8홀, 원본의 1홀 학교 코너, 2층 편의시설 뷰를 유지한다.
- 후속 요청으로 추가한 9–11홀, 9홀 인디·SELECTED INDIE 80·비즈니스 확대도도 유지한다. 현재 7개 도면, 557개 부스다.
- 휴대전화 전시관·시설 메뉴는 하단이다. 시연은 게임패드, 정리권은 티켓, 판매는 쇼핑백 SVG를 부스 오른쪽 위 모서리에 작게 표시한다. 중앙 문자 배지로 되돌리지 않는다.
- 상세 정보는 공식 출처와 확인 날짜를 기록하고, 미확인을 없음으로 간주하지 않는다. 개별 조사 원본은 sources/booth-details.json이며 scripts/build-booth-details.cjs로 지도 데이터에 병합한다.
- 500ms 길게 누르기로 관심 부스 등록/해제, 기존 저장 키, 검색, 화면 모드, 편의시설 강조를 유지한다.
- 사용자 PC는 Windows, 대상 기기는 iPhone 15 Pro Max. 세로·가로·PC 모드를 유지한다.
- 페이지 스크롤 없이 화면에 맞추고 지도 내부의 확대·이동은 허용한다.
- `npm run dev`로 실행, `npm test`로 기존 모의 DOM 검증을 할 수 있다. 별도 npm 의존성 설치는 필요 없다.
- 모의 DOM 테스트나 데스크톱 미리보기를 실제 iPhone/iOS/AR 검증으로 보고하지 않는다.
- GPS·방향 센서와 통로 A* 안내는 NAVIGATION.md에 따라 구현했다. 멧세 정합은 근사치이며 현장 iPhone 검증은 미완료다. 카메라 AR와 네이티브 앱은 미구현이다.
- 대상 환경의 도구와 실제 iOS 실행 가능 여부부터 확인한다. 이전 세션의 도구 연결 상태를 현재 상태로 단정하지 않는다.
- 이관 자체를 웹 AR 방식이나 네이티브 프레임워크 선택에 대한 확정 결정으로 해석하지 않는다.
