# TGS 위치·통로 안내 통합 — 2026.09.16.1

## 2026-09-16 수정

- SVG 테두리의 `hidden` 속성을 실제 DOM에서 제거/추가한다. 이전 코드는 SVG에 HTML용 hidden 프로퍼티를 대입해 테두리가 계속 숨겨지는 문제가 있었다.
- 확대·드래그·핀치 시 자동 전시관 맞춤을 해제한다. 리사이즈 때 사용자 확대/이동을 되돌리지 않으며 포인터 캡처 실패 시 창 단위 입력으로 이어간다.
- 일반 화면은 하단 관심/전시관/시설 메뉴를 숨기고 펼칠 수 있다. 안내 중에는 버튼과 상태 문구를 접되 목적지 부스명·종료 버튼·지도·화살표·테두리는 유지한다.
- 안내 시작/내 위치 버튼은 내 위치를 확대하여 따라간다. 사용자가 드래그/핀치하면 따라가기를 멈추고 `내 위치 따라가기`로 복귀한다. `전체 경로`는 별도 동작이다.
- 강제 화면 회전에서도 방향 화살표의 프레임 회전을 보정한다. 방향 센서 정보가 없으면 점과 방향 권한 버튼을 표시한다.
- 멧세 밖 GPS를 임의의 부스 좌표에 저장하지 않는다. 멀리서 통로를 지정하면 수동 위치로 안내하며, 실제 위치 추적으로 표시하지 않는다.
- 회귀 검증에 확대 후 드래그/핀치/리사이즈, 포인터 캡처 실패, 메뉴 접기, 센서 이동에 따른 따라가기/수동 이동, 실제 SVG 속성과 빨강·주황·초록 스타일 및 길이 변화를 추가했다. 실물 iPhone 테스트는 아직 수행하지 않았다.

## 사용

- 첫 화면 `멧세 전체`는 실제 멧세 건물과 보행 통로에 공식 TGS 도면의 부스 557개를 배치한다. 기존 공식 도면 7개도 유지한다.
- `내 위치`에서 위치·방향 접근을 허용한다. 부스를 더블탭/더블클릭하면 안내를 시작한다. 같은 부스를 다시 더블탭하면 해제하며 다른 부스는 목적지를 바꾼다. PC는 부스에 초점을 두고 Shift+Enter, 종료는 Escape도 지원한다.
- 안내 중 일반 메뉴는 숨기고 지도, 내 위치, 목적지 부스 정보와 최소 안내 조작만 표시한다. 부스까지의 숫자 거리·오차는 표시하지 않는다.
- 현재 위치 아이콘 자체가 굵은 방향 화살표다. 방향 센서가 없거나 오래된 경우 방향을 지어내지 않고 점으로 표시한다.
- 하이라이트는 남은 통로 길이에 따라 빨강(150m 이상), 주황(30m 초과), 초록(30m 이하)이다. 길이는 화면 둘레의 8~55%로 제한하며 150m부터 15m까지 가까워질수록 늘어난다.
- GPS가 도면과 어긋나면 `내 위치 맞추기`를 누르고 실제 서 있는 통로를 선택한다. 이는 출발점 보정이며 빈 지도 더블탭은 목적지를 만들지 않는다.
- 길게 누르는 관심 부스 등록/해제, 검색, 부스 상세와 출처, 시설, 세로·가로·PC 모드는 유지한다.
- 이동 궤적을 수집하거나 저장하지 않는다. 저장하는 새 데이터는 도면 위치 보정값뿐이다. 정보 창의 위치 초기화는 이 값만 지우고 관심 부스를 보존한다. 별도 범어역 테스트 사이트 저장소는 이 사이트에서 접근하거나 삭제할 수 없다.

## 좌표와 통로의 한계

실제 OSM 건물 외곽과 공식 시설 치수에 도면을 맞춘 **근사 정합**이다. 측량된 부스 GPS 좌표가 아니다. 실내 GPS 오차는 웹앱만으로 없앨 수 없으며 한 점 보정이 필요할 수 있다. 현재 실제 iPhone/현장 GPS·나침반 테스트를 완료한 상태는 아니다.

1–8홀, 9–11홀, 2F 에스플러네이드와 실제 2F 연결교를 함께 표시한다. 층은 한 화면에 펼쳐 놓되 경로는 지정한 계단 연결만 사용한다. 휠체어 접근성 경로/엘리베이터 경로를 보장하지 않는다. 행사장의 임시 차단과 일방통행은 실시간 반영하지 않는다. 공식 TGS 안내의 일반공개일(9/19~21) 오전 10시 전 홀 간 이동 제한은 연결교 경로를 점선과 안내 문구로 표시하고 테두리 안내를 숨기는 방식으로 반영했다.

공식 부스 도면의 여백을 모두 길로 취급하지 않는다. 통행 영역을 벡터로 구성하고 부스·무대 장애물을 뺀 뒤 보수적으로 래스터화한다. A*와 모서리 통과 차단을 사용하며 목적지는 부스 내부가 아니라 부스 바깥 통로다. 출발점이 통로 밖이면 잘못된 직선을 그리지 않고 위치 보정을 요청한다.

## 재사용 구성

- `dist/navigation/core.js`: 독립 좌표 변환, 나침반, 색/길이 계산, 격자 A*, 경로 단순화.
- `sensors.js`: 사용자 제스처에서 iOS 방향 권한 요청, GPS watch, 오래된 센서 판정, 백그라운드 중지/복귀.
- `route-worker.js`: 큰 통로 격자의 경로 계산을 Web Worker로 분리.
- `tgs-navigation.js`, `navigation.css`: TGS 앱 연결과 안내 화면.
- `campus.json`, `campus-grid.json`, `../campus.svg`: 멧세 좌표·부스 배치·통행 영역·벡터 지도.
- `walkable.json`: 기존 공식 도면별 통로 정의.
- `sources/navigation/registration.json`: 근사 정합 기준 및 원본 사각형 대응.
- `scripts/build-campus.py`: OSM과 공식 도면 데이터로 위 멧세 자산 재생성. Python, numpy, shapely가 필요하다. 런타임은 외부 npm 의존성이 없다.

HTTPS 배포에서 센서를 사용한다. PC localhost는 개발용으로 사용할 수 있지만 휴대전화에서 일반 HTTP LAN 주소는 위치·방향 권한 테스트용이 아니다. 서비스 워커 버전과 HTML 버전은 함께 변경한다.

추출 ZIP의 `portable/`에는 테스트 지도 없는 독립 안내 모듈을, `tgs-integration/`에는 기존 TGS 앱에 적용한 파일과 정합 원본을 담았다. `integration.patch`는 기준 커밋 062dd76에 적용할 전체 변경이다. 다른 버전에는 자동 덮어쓰기 대신 차이를 검토해야 한다. 추출본만으로 원래 7개 공식 SVG 등 전체 사이트 자산을 대체하지 않는다.

## 검증

`npm test`: 기존 관심·검색·시설·부스 상세·레이아웃 동작, 서버, 서비스 워커 캐시, 원본 557개 부스 접근 경로, 벽·모서리 차단, 멧세 홀 사이 연결교 경로, 센서 허가/중지, 더블탭 시작·전환·해제를 검증한다. DOM과 센서는 모의 환경이다. 실제 브라우저/iPhone 실행 검증으로 해석하지 않는다. 멧세 SVG는 별도로 렌더링하여 육안 확인했다.

## 출처와 저작권

- TGS 공식 도면: https://service.tgs.cesa.or.jp/2026/venue_pdf_en/
- 공식 시설도: https://www.m-messe.co.jp/docs/facility/messe_facilityguide.pdf
- 홀 1–8 치수: https://www.m-messe.co.jp/organizers/guide/exhibitionhall/guide_exibition1-8
- 홀 9–11 및 연결교: https://www.m-messe.co.jp/organizers/guide/exhibitionhall/guide_exibition9-11
- 행사 이동 제한: https://tgs.cesa.or.jp/2026/en/caution
- 지리 원본: © OpenStreetMap contributors, https://www.openstreetmap.org/copyright (ODbL). `sources/navigation/makuhari.osm`에 원본을 보존한다. 파생 지리 데이터 사용 시 ODbL 조건과 지도 출처 표기를 유지해야 한다.

TGS 부스 도면과 시설 PDF는 각 권리자의 자료이며 이 추출본이 해당 자료의 별도 재배포·상업 이용 허락을 부여하지 않는다.
