# 最新 수정: standalone 높이와 편집용 콘텐츠 — 2026-09-17

standalone 기본/동일 방향 모드에서는 JS 높이 px 대신 CSS 100dvh/100%가 프레임을 채운다. 키보드와 강제 회전/PC 모드는 유지한다. viewport-fit=cover와 기존 safe-area 패딩은 유지한다. 상세 정보 34개는 dist/data.json으로 이동했으며 booth/game/goods/image/condition과 기존 상세 필드를 보존한다. 지도 형상은 map-data.json, 내용은 data.json이다. 기존 build-booth-details 스크립트는 과거 추출용이며 수동 콘텐츠 편집 후 다시 실행하지 않는다.

정적 사이트의 실제 모바일 브라우저 미리보기는 현재 실행 환경에서 지원하지 않는다. 따라서 검증은 모의 DOM/센서/viewport이며 실제 iPhone standalone 렌더링 정상 확인을 완료했다고 표현하지 않는다.

# 최신 UI: 메뉴 상단 화살표 — 2026-09-16

하단 메뉴 숨기기는 지도/하단 메뉴 경계 중앙의 아래 화살표 탭으로 변경했다. 접으면 위 화살표로 복원한다. 휴대전화 헤더·관심 목록·시설 메뉴의 안쪽 여백을 줄였다. iOS 안전 영역은 유지하며 ChatGPT 닫기/공유/사이트 편집 영역은 호스트 UI라 수정하지 않는다.

# 최신 수정: 터치·안내 표시 — 2026-09-16

NAVIGATION.md의 2026-09-16 수정 항목을 참고한다. SVG 숨김 오류, 확대/드래그 유지, 내 위치 따라가기/전체 경로 분리, 하단 메뉴 접기를 수정했다. 테스트는 모의 DOM/센서이며 실제 iPhone 테스트로 표현하지 않는다.

# 최신 변경: 멧세 통합 안내 — 2026-09-15

NAVIGATION.md가 현재 안내 구현의 기준이다. 기존 도면 7개와 관심/상세 기능을 유지하며 실제 멧세 지리 위에 근사 정합한 전체 지도와 2F 연결교 경로를 추가했다. 부스 더블탭 안내, 방향 화살표, 빨강/주황/초록 테두리, 한 점 위치 보정이 있다. 이동 궤적과 거리/오차 수치는 없다. 카메라 AR는 미구현, 실내 GPS 정확도와 실제 iPhone 동작은 검증 전이다. 아래 내용의 미구현 GPS/경로 언급은 과거 기록이다.

# 최신 변경: 여행용 v2 — 2026-09-12

9–11홀·인디 확대도, 게임패드/티켓/쇼핑백 모서리 아이콘, 하단 메뉴, 전시·굿즈·판매 상세를 추가했다. 도면 7개, 부스 557개, 시설 68개, 공식 정보 레코드 34개다. 상세 변경·검증은 V2_VALIDATION.md, 설치는 TRAVEL_START_HERE.md를 참고한다. 아래 v1 및 최초 이관 내용은 과거 기록이다.

# 작업 인계 — 2026-09-11

## 최신 변경: 여행용 중간 완성본 v1

최신 요청은 “지금까지 한 것을 일본에서 혼자 별도로 사용할 수 있는 중간 최종작업물로 만들기”입니다. AR 후속 개발보다 현재 지도 기능의 독립 사용을 우선했습니다.

- `dist/manifest.webmanifest`, `sw.js`, `travel.js`, `travel.css`, 설치 아이콘을 추가했습니다.
- HTTPS 홈 화면 앱에서 3개 지도와 정적 자산 전체 저장을 확인합니다. 관심 목록 JSON 백업/복원은 기존 저장 키를 사용합니다.
- `TRAVEL_START_HERE.md`에 설치·출국 전 비행기 모드 실기기 확인·관심 목록 이전을 안내합니다.
- 개발 서버(5173)는 실시간 새로고침, 여행용 정적 실행은 `npm start`로 분리했습니다. 개발 HTML은 오프라인 캐시에 넣지 않습니다.
- 모의 DOM·서버·서비스 워커 시뮬레이션 검증은 실제 iPhone 테스트가 아닙니다. 실제 홈 화면 설치와 비행기 모드 검증은 아직 사용자 확인이 필요합니다.
- 동결 소스는 `releases/travel-v1-source`에 보관합니다. 원래 `dist/`에서 후속 개발을 계속해도 재배포하지 않는 한 배포본에 반영되지 않습니다.
- 다음 배포 때 **`dist/sw.js`의 VERSION과 `dist/index.html`의 tgs-travel-version을 함께 올리세요.** 기존 캐시를 덮어쓴 같은 버전 재배포는 피합니다.

아래는 최초 이관 시점의 기록입니다. PWA 미구현 등의 과거 상태는 위 최신 변경으로 대체됩니다.

## 이관 목적과 최신 대화

사용자는 TGS 지도를 Codex에서 계속 개발하려 한다. 개발 도구 설정·수동 테스트 부담을 줄이고,
AI가 직접 앱을 만들고 실행 화면을 확인하며 수정하기를 원한다. 현재 PC는 Windows다.
최신 지시: “지금까지한 작업을 코덱스로 이관시키고싶은데”.
이 사본은 소스와 작업 맥락을 전달한다. Codex 세션 생성·사용자 PC의 폴더 열기는 수행하지 않았다.

## 완료한 웹앱

공식 TGS 2026 영문 지도 PDF를 사용했다. 메인 전시관 1–8홀만 부스 선택 대상으로 삼았다.
1홀 학교 코너는 공식 별도 도면에 따라 별도 뷰, 2층 Central Mall은 시설 뷰다.
상품관·다른 홀·회의실 부스는 범위 밖이다. 개별 회사 부스 내부 게임별 위치는 공식 전체 도면만으로 확인되지 않았다.

- 메인 지도 160개 + 학교 코너 67개 = 227개 부스. 공식 번호와 원본 사각형을 일대일 대응했다.
- 500ms 길게 누르면 관심 부스로 등록, 다시 길게 누르면 해제한다.
- 8px 초과 이동, 핀치, 포인터 취소 등은 길게 누르기를 취소한다. 짧은 탭은 상세 표시다.
- 관심 목록은 localStorage에 저장한다. 빈 목록도 다음 실행에서 유지한다.
- 부스 이름·전시사·부스 번호 검색, 일부 한국어 별칭, `7-c4` 같은 번호 표기 정규화.
- 검색 결과 및 관심 목록 페이지 넘김. 결과 선택 시 해당 도면·부스로 이동한다.
- 화면 모드: 자동, iPhone 세로, iPhone 가로, PC. 화면 방향과 강제 모드가 다르면 프레임을 회전한다.
- iPhone 기준 CSS 영역 430×932 / 932×430. 기기 픽셀 해상도와 CSS 크기는 구분한다.
  Safari 주소창·safe area 때문에 실제 사용 가능한 뷰포트는 동적으로 계산한다.
- 페이지 스크롤을 막고, 지도 내부 팬·핀치·휠·줌 버튼을 지원한다.
- 편의시설 48개: 입출구 4, 보관함 5, 음식 3, 충전 1, 인포메이션 1, 화장실 34.
- 같은 종류의 가까운 시설은 개수와 함께 묶어 표시한다.
- 입장 안내는 공식 도면의 날짜·시간·진행 방향 조건을 보존한다. 출구와 입구를 혼동하지 않는다.
- 지도는 PDF 원본을 SVG로 추출해 인라인 SVG 장면에서 확대한다. AI 업스케일을 사용하지 않았다.
  원본 글자·선이 벡터이므로 확대 시 재래스터화되도록 했다.
- PDF 내 CMYK 배터리 아이콘을 원본 리소스에서 RGB PNG로 변환해 SVG에 삽입했다.

## 사용자 관심 부스

이전 방문 계획에 따른 기본 관심 부스:

| 업체/전시 | 코드 |
|---|---|
| 반다이남코 | 05-N01 |
| NEXON | 07-C04 |
| Astrae Oratio / NC 관련 전시 | 07-C03 |

CAPCOM 07-S01은 후보로 거론됐지만 기본 관심 부스에는 넣지 않았다.
사용자가 실제 기기에서 추가·해제한 기록은 서버에 저장하지 않아 이 파일로 가져올 수 없었다.

## 파일·좌표 구조

`dist/app.js`가 이벤트·데이터·상태·검색·지도·화면 모드를 관리한다.
`dist/style.css`가 고정 뷰포트 및 반응형 UI, `dist/index.html`이 진입점이다.
`dist/map-data.json`에 maps/booths/facilities가 있다. 모든 좌표는 각 도면 내부 좌표다.

| 도면 | 내부 크기 | 원본 PDF crop |
|---|---|---|
| main | 938×186 | [180, 60, 1118, 246] |
| school | 258×182 | [850, 334, 1108, 516] |
| concourse | 938×134 | [180, 246, 1118, 380] |

지도 SVG에는 원본 글자 경로와 변환이 포함된다. 이미지 전체 CSS 확대 대신 SVG 장면을 변환한다.
포인터 좌표 변환은 SVG getScreenCTM을 사용한다.

저장 키:
- `tgs2026-interest-booths-v1`
- `tgs2026-screen-mode`

## 검증 상태

이관 전 README에 기록된 검증:
- JavaScript 문법, 정적 진입점, 로컬 리소스, 227개 부스·번호 대응.
- 원본 도면 crop 렌더링 및 시각 확인.
- 모의 DOM으로 길게 누르기 등록/해제, 짧은 탭, 드래그/핀치/취소, 저장 복원, 빈 관심 목록 유지.
- 이름·번호·한국어 검색, 결과 페이지, 시설 뷰 이동, 입장 조건.
- 세로 430×932, 가로 932×430, PC 1440×900 및 강제 회전 프레임.
- 모든 부스 및 시설 좌표가 도면 범위 안에 있는지 확인.

이관 시 `tests/check-interactions.cjs`의 고정 Linux 경로를 상대 경로로 고쳐 다시 검증한다.
이 테스트는 일부 DOM을 모의한 단위·상호작용 검증으로, CSS 실제 배치·Safari·터치 하드웨어를 검증하지 않는다.
실제 브라우저, iPhone 실기기, iOS 시뮬레이터 QA는 기존 작업에서 수행하지 않았다.
선택적 WebMCP 등록 역시 지원 브라우저에서의 검증은 하지 않았다.

## 미구현·미확정

다음은 논의만 했으며 현재 소스에 구현되지 않았다:
- 카메라 화면 위 AR 지도·방향 표시.
- GPS/나침반 기반 실시간 위치 추적, 정확도 표시.
- 전시관 지도와 위경도의 정합, 통로 그래프, 층간 이동을 포함한 경로 계산.
- 실제 주변 풍경을 인식하는 VPS 보정.
- SwiftUI/ARKit/RealityKit 네이티브 프로젝트, 서명, 빌드, 설치.
- 앱 설치용 PWA manifest/service worker 및 명시적인 오프라인 캐시.

사용자는 GPS와 카메라 환경 인식을 조합한 구글 지도 방식의 위치 보정을 원리로 들었다.
단순 방향 센서 오버레이와 VPS/월드 고정 AR을 구분해야 한다.
공식 2D 도면만으로 실내의 정확한 절대 위치가 자동으로 생기지 않는다.
TGS 임시 부스 환경과 마쿠하리 멧세 내부의 VPS 커버리지는 확인되지 않았다.
현장 정확도를 확인하지 않고 센티미터급 위치나 안정적인 AR 길 안내를 보장하면 안 된다.
웹앱 방식 또는 네이티브 방식의 최종 선택은 아직 확정되지 않았다.

## 개발·테스트 환경 논의

이관을 만든 세션은 Linux였고 xcodebuild/xcrun/Swift 및 연결된 iOS 시뮬레이터가 없었다.
클라우드 iOS 실행 환경 연결 플러그인도 찾지 못했다. Replit 플러그인은 검색됐지만 설명은 웹앱용이었고 연결하지 않았다.
새 Codex 환경에서는 가용 도구를 다시 확인해야 한다.
Windows의 Codex를 연다는 사실만으로 iOS 시뮬레이터나 ARKit 실행 환경이 생기지는 않는다.

앞서 조사한 후보의 공식 문서(가용성·요금은 사용할 때 다시 확인):
- Rork native iPhone: https://docs.rork.com/iphone/rork-iphone
- Rork simulator: https://docs.rork.com/features/simulator
- Replit mobile: https://docs.replit.com/features/artifact-types/building-mobile-apps
- Replit App Testing: https://docs.replit.com/features/agent/app-testing
- XcodeBuildMCP: https://www.xcodebuildmcp.com/
- Appetize: https://docs.appetize.io/

이 후보들을 현재 Codex가 연결·제어하고 있다고 말하지 않는다.
네이티브 iOS 자동 테스트와 웹앱 자동 테스트의 지원 범위를 구분한다.
실제 카메라/GPS/현장 AR은 물리 기기 확인이 필요하다.

## 배포·소스 기준점

기존 사이트: https://tgs-2026-interest-map.fggfgggcx.chatgpt.site
기존 배포는 소유자 전용이었다. 이관 작업은 배포나 공개 범위를 변경하지 않았다.

- source commit: `0c2cff2fd6134be715eb8a3d5460d586fdc1d45a`
- project ID: `appgprj_6aa25c0ab504819198af2f6b15855447`
- source version: `appgprj_6aa25c0ab504819198af2f6b15855447~appgver_8e382ef8310c81919657f9d7367afff8`
- recorded successful deployment: `appgdep_6aa388c66e28819185a2dfb4d97c7e7b`

원본은 Sites 관리 Git 저장소에 있다. 이관본에는 Git 인증·세션·토큰을 포함하지 않았다.
원래 `.openai/hosting.json`은 `handoff/original-hosting.json`으로 보존했다.
이 파일을 로컬에서 실행하는 데 기존 배포 서비스 연결은 필요 없다.
기존 배포를 갱신하려면 해당 서비스 접근 권한과 도구가 별도로 필요하다.
다른 주소에서 실행하는 것만으로 기존 사이트 또는 그 주소의 localStorage가 갱신되지는 않는다.

## 다음 담당자의 첫 작업

1. 현재 폴더의 런타임을 확인하고 `npm run dev` 또는 Python HTTP 서버로 실행한다.
2. `npm test`를 실행한다. 도구가 있다면 브라우저에서 세로·가로·PC 화면을 실제 확인한다.
3. 기존 기능 상태와 현재 환경의 iOS 빌드·시뮬레이터 접근 가능 여부를 짧게 보고한다.
4. 미확정 구현 방식을 사실에 근거해 좁히고 AR/위치/경로 후속 작업을 이어간다.
   GPS·VPS·실내 좌표가 없는 상태에서 가짜 현재 위치를 실제 위치로 표시하지 않는다.

## 공식 지도 출처

- 안내 페이지: https://tgs.cesa.or.jp/2026/en/map
- 공식 PDF 다운로드: https://service.tgs.cesa.or.jp/2026/venue_pdf_en/
- 원본 PDF는 `sources/`에 포함되어 있다.
